; Applied via PHP_INI_SCAN_DIR to all PHP CLI subprocesses during build/deploy.
; This ensures Composer post-install hooks (assets:install, cache:warmup, etc.)
; are never killed by the default 128MB PHP memory limit.
memory_limit = -1
max_execution_time = 0
